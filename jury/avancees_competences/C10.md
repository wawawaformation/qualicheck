---
competence: C10
titre: Intégrer l'API d'un modèle ou d'un service d'intelligence artificielle dans
  une application
bloc: Bloc 2 — Intégrer des modèles et des services d'intelligence artificielle
criteres:
- Application de départ installée et fonctionnelle en développement
- Communication avec l'API fonctionnelle
- Authentification et renouvellement (expiration des jetons) intégrés correctement
- Tous les points de terminaison concernés intégrés selon les spécifications
- Adaptations d'interface intégrées en accord avec les spécifications
- Tests d'intégration couvrant tous les points de terminaison exploités, sans bug
- Sources versionnées, dépôt Git de l'application
---

## Avancées

- État au 2026-07-21 (`jury/README.md`) : non conçu.
- **2026-08-29** — Maquettes de connexion/profil préparées (`conception/maquettes/utilisateur/`) en anticipation d'un futur client consommant `api_business` — aucun code Vue.js réel à ce stade, cf. mémoire de session [[focus_api_avant_client]].
- **2026-09-11** — Trois clients LLM intégrés dans `app/retrieval/` (Azure OpenAI via `langchain_openai.ChatOpenAI`, sortie structurée `JsonOutputParser`) : `DecompositionClient` (existant), `JugementClient` (jugement de pertinence, intégré dans `POST /regles/dense`) et `GuardrailClient` (classification périmètre, mesuré isolément, non encore branché). Retry 3 tentatives + fail-open documenté et testé pour chacun. Tests d'intégration `tests/integration/api_regles/test_regles_dense.py` couvrant le point de terminaison exploité (filtrage effectif, refus `[]`, pannes techniques → 503), sans bug.
- **2026-09-16** — Configuration des 3 clients réorganisée par domaine réel d'usage (spec `docs/superpowers/specs/2026-09-16-config-llm-par-domaine-design.md`, plan associé, 5 tâches exécutées en inline, carte Kanboard #18) : `app/retrieval/config.py`/`config.yml` créé (n'existait pas — les rôles `decomposition`/`jugement`/`guardrail`/`rag_acceptance` vivaient jusque-là dans `app/ingestion/`, faute de module dédié), `load_manifest()`/`manifest.yml` renommés en `load_config()`/`config.yml` partout (18 fichiers Python migrés, coupure nette, aucune régression — 263 tests verts avant/après). Paramètre `temperature` explicite ajouté au passage (voir C9).
- **2026-09-19 / 2026-09-20** — Agent US2 « nu » (marche A1, carte #23) : boucle ReAct en tool-calling natif LangChain (`bind_tools`), module `app/agent_us2/`, appelable en CLI et par API — `A_agent_nu/A1_boucle_et_premier_outil.md`. Marche A4 (carte #26) : table `OUTILS`, outils `lire_regle` et `rechercher_regles` alignés sur l'API des règles ; un nom d'outil inventé par le LLM ne lève plus de `KeyError` (l'agent reçoit `{"statut": 404, "erreur": "Outil inconnu : ..."}`), comportement vérifié avec le vrai modèle (gpt-5.4-mini) contre l'API des règles réelle.
- **2026-09-20** — Second usage d'un LLM, cette fois comme outil de développement : `scripts/review_pr.py` relit le diff d'une PR. Sortie JSON validée par Pydantic (verdict `ok`/`mineur`/`bloquant`), retry 3 tentatives avec backoff, coût de chaque appel calculé et affiché.
