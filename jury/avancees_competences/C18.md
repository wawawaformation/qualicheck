---
competence: C18
titre: Automatiser les phases de tests du code source
bloc: Bloc 3 — Réaliser une application intégrant un service d'intelligence artificielle
criteres:
- Documentation couvre outils, étapes, tâches, déclencheurs
- Outil d'intégration continue cohérent avec l'environnement technique
- Chaîne intègre toutes les étapes préalables aux tests (build, configurations)
- Chaîne exécute les tests disponibles au déclenchement
- Configuration versionnée avec les sources, dépôt Git distant
- Documentation d'installation, configuration, test, accessible
---

## Avancées

- `.github/workflows/ci-dev.yml` opérationnel : lint (`ruff`), migrations testées contre un Postgres éphémère, tests unitaires + intégration à chaque push hors `main`/`staging`.
- **2026-08-29** — Piste Gitea Actions engagée (compatibilité ~90 % avec la syntaxe GitHub Actions vérifiée), essai non destructif prévu, GitHub reste actif entre-temps : `jury/decisions/2026-08-29-hebergement-git-gitea.md`.
- **2026-08-30** — `ci-dev.yml` validé réellement sur `git.david-legrand.fr` (`.gitea/workflows/`), après correction d'une différence de comportement réseau (`services:` Postgres) entre runner GitHub-hébergé et `act_runner` en mode conteneur.
- **2026-09-20** — `ci-feature.yml` créé (lint, tests unitaires et intégration avec LLM mocké, sur toute branche hors `main`, `staging` et `dev`) ; `ci-dev.yml` restreint au seul push sur `dev` ; miroirs GitHub ajoutés. `ci-review.yml` (déclenché par une PR vers `dev`) poste une revue de diff par LLM en commentaire : seul un verdict `bloquant` fait échouer la CI, une panne de l'outil de revue reste non bloquante. `ci-feature` validée en réel sur la branche `feature/revue-pr-llm` ; `ci-review.yml` pas encore éprouvée par une vraie PR (carte #51). Doublon probable `ci-feature` / `ci-dev` à arbitrer.
