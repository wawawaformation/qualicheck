---
competence: C20
titre: Surveiller une application d'intelligence artificielle
bloc: Bloc 3 — Réaliser une application intégrant un service d'intelligence artificielle
criteres:
- Documentation liste métriques, seuils et valeurs d'alerte par métrique à risque
- Documentation explicite les arguments des choix techniques
- Outils (collecteurs, journalisation, agrégateurs, filtres, dashboard) installés
  et opérationnels au moins en local
- Règles de journalisation intégrées selon les métriques à surveiller
- Alertes configurées et en état de marche
- Documentation d'installation/configuration, accessible
---

## Avancées

- Relève d'US1/US2, non actives à ce stade (`jury/README.md`, état au 2026-07-21).
- Candidat externe pour la partie monitorage technique : `benchmark-azure/` (hors périmètre strict de C20, qui porte sur *une application*, pas sur l'infra LLM).
- **2026-09-16** — Endpoint `/version` (`app/api_regles/main.py`) enrichi : expose désormais `commit` (SHA complet, précis mais peu lisible — friction concrète rencontrée en diagnostiquant le déploiement staging du même jour, cf. C19), `deployed_at` et `version` (contrat d'API, `app/api_regles/config.yml::api.version`, repère lisible et communicable). Version bumpée `0.1.0` → `0.2.0` : restée figée depuis le début malgré plusieurs changements réels de contrat, rattrapée en un seul saut plutôt que de reconstituer un historique approximatif — règle actée pour la suite (bumper à chaque changement de contrat d'endpoint observable). Vérifié en conditions réelles : `curl https://regles.qualicheck.koabana.fr/version` recoupé avec le commit de déploiement attendu. Test d'intégration étendu (`test_version_expose_le_commit_la_date_et_la_version_du_contrat`).
