---
competence: C1
titre: Automatiser l'extraction de données
bloc: Bloc 1 — Collecte, stockage et mise à disposition des données
criteres:
- Script d'extraction fonctionnel — toutes les données visées sont récupérées à l'exécution
- Le script comprend point de lancement, initialisation des dépendances/connexions,
  règles de traitement, gestion des erreurs/exceptions, fin de traitement, sauvegarde
  des résultats
- Script versionné, accessible depuis un dépôt Git
- 'Extraction faite depuis un mix d''au moins : service web (API REST), fichier de
  données, scraping, base de données, système big data'
---

## Avancées

- **2026-07** — `app/ingestion/acquisition.py` : `fetch_api()` (API REST Opquast) + `scrape_rule()` (scraping BeautifulSoup, champs `solution`/`controle` non exposés par l'API).
- **2026-07-26** — Ré-ingestion complète réelle exécutée sur les 245 règles (prompt V6).
- **2026-07-28** — Couverture assumée à 2 sources sur 5 (API + scraping), justification préparée pour l'oral plutôt qu'extension artificielle : `jury/decisions/2026-07-28-couverture-sources-extraction-c1.md`.
