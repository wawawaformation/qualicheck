---
competence: C21
titre: Résoudre les incidents techniques
bloc: Bloc 3 — Réaliser une application intégrant un service d'intelligence artificielle
criteres:
- Causes du problème identifiées correctement
- Problème reproduit en environnement de développement
- Procédure de débogage documentée depuis l'outil de suivi
- Solution documentée, explicitant chaque étape de la résolution
- Solution versionnée dans le dépôt Git (ex. merge request)
---

## Avancées

- Plusieurs incidents réels documentés dans `docs/problemes_rencontres/` (ingestion) : dérive de prompt engineering, dimensionnement des colonnes texte, disqualification du modèle d'embedding MiniLM (`6_embedding_minilm_disqualifie.md`).
- **2026-08-28** — Incident réel résolu en session : échec du déploiement staging dû à un décalage entre la fixture d'acceptance et la reclassification réelle du prompt V6 — cause identifiée, reproduit, corrigé, tracé dans le commit `test: sync api_regles acceptance counts with V6 reclassification`.
- Incident HTTP 401 du projet externe `benchmark-azure/` : constaté mais pas encore rédigé selon les critères C21 (cause/reproduction/solution) — cf. `TODO.md`.
- **2026-08-30** — 3 incidents réels résolus en session lors de la mise au point du déploiement SSH sur Gitea (script distant tronqué silencieusement par `docker compose exec`, `PATH` absent pour `uv` en SSH non-interactif, secrets Postgres/API désynchronisés de la base réelle) — cause, reproduction et solution documentées dans `docs/problemes_rencontres/deploiement/1_ssh_deploy_gitea.md`.
- **2026-09-20** — Incident réel : premier run de l'acceptance sur tag (run Gitea 68) échoué alors que 6 familles sur 7 étaient à 95-100 %. *Cause* : `sans_reponse` à 0/20 comparée au seuil de 90 % ; `check_rag_acceptance.py` la déclarait « hors seuil » pour le seul libellé du log, tandis que `is_acceptable` l'inclut depuis le guardrail du 2026-09-13 (juste pour l'API des règles, faux pour le retrieval seul qui ne peut pas refuser). *Reproduction* : rejeu des taux réels du run 68 dans `is_acceptable`, refus avant correction. *Solution* : paramètre optionnel `familles_exclues` (comportement par défaut inchangé), test écrit d'abord ; rejeu accepté après. Pas encore rédigé dans `docs/problemes_rencontres/`.
- **2026-09-20** — Passe de correction de 16 défauts après revue de la marche A2, dont un endpoint Langfuse jamais vérifié en réel (`/api/public/otel` corrigé en `/api/public/otel/v1/traces`) ; nom d'outil inventé par le LLM (marche A4) qui levait un `KeyError`, désormais géré et vérifié avec le vrai modèle.
