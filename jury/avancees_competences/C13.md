---
competence: C13
titre: Créer une chaîne de livraison continue d'un modèle d'intelligence artificielle
bloc: Bloc 2 — Intégrer des modèles et des services d'intelligence artificielle
criteres:
- Documentation couvre toutes les étapes, tâches, déclencheurs
- Déclencheurs intégrés comme définis
- Fichiers de configuration reconnus et exécutés correctement
- Étapes de test des données, d'entraînement et de validation intégrées, sans erreur
- Étape de livraison (ex. pull request) intégrée avec rapports d'évaluation attachés
- Sources versionnées, dépôt Git distant
---

## Avancées

- `rag-acceptance` volontairement hors CI pour l'instant (coût réel par appel LLM, même s'il s'est révélé négligeable : ~0,0002 €).
- **2026-08-29** — Principe posé de penser la validation automatisée du comportement LLM dès la conception des US, pas après coup (mémoire de session [[penser_ci_modele_des_conception]]) — pas encore intégré à un pipeline réel.
- **2026-09-20** — Pipeline d'acceptance déclenché par tag, décidé puis implémenté (carte #45, `conception/4_ci_cd/strategie_tests_et_gates.md`) : le tag manuel `YYYY-MM-DD-<sha7>` est la précondition de promotion et lance `ci-acceptance.yml` (Postgres éphémère, import de `tests/fixtures/referentiel_embedde.sql` pour ne pas refaire les 245 appels LLM d'ingestion, puis `check_rag_acceptance.py` et les deux acceptances de l'API des règles), séparé de `ci-feature.yml` (LLM mocké). Coût estimé de `check_rag_acceptance.py` dans le run réel : environ 0,008 €. Remplace la mention « hors CI » ci-dessus.
- **2026-09-20** — Premier run réel (tag `2026-09-20-fb62e53`, run Gitea 68) : le workflow démarre, le filtre de tag est validé, l'acceptance échoue sur `sans_reponse` (incident et correction : voir C21). Gate à seuil `dev -> staging` décidé, pas encore implémenté ; nouveau tag à poser pour revalider (carte #51).
